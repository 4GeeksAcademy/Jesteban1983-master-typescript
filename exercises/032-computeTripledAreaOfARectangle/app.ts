function computeTripledAreaOfARectangle(length: number, width: number): number {
  let result = (length*width)*3;
  return result;
}
let output=computeTripledAreaOfARectangle(2,4);
console.log(output);
export {};
